# Backend (server.ts) - Copy to .env and fill with real values
PUSHER_APP_ID=your-app-id-here
PUSHER_KEY=your-key-here
PUSHER_SECRET=your-secret-here
PUSHER_CLUSTER=eu
PORT=3000

# Frontend - Copy to .env.local and fill with real values
# VITE_PUSHER_KEY=your-key-here
# VITE_PUSHER_CLUSTER=eu
# VITE_PUSHER_ENDPOINT=/trigger  # Optional - defaults to '/trigger'
